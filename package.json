{
  "name": "prism",
  "version": "0.1.0",
  "private": true,
  "description": "PRISM — Projected Reality Interaction & Spatial Manipulation (ZYNASH LABS)",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "vendor:offline": "node scripts/vendor-offline.mjs",
    "package:usb": "npm run vendor:offline && npm run build"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.3.0",
    "@mediapipe/tasks-vision": "1.0.1",
    "three": "0.186.0"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "typescript": "7.0.2",
    "vite": "8.3.0",
    "vitest": "5.0.1"
  }
}
